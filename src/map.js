import rfapi from './index'

export default class {
    constructor(uuid, options) {
        this.mapid = uuid
        this.options = options
        this.nodes = {}
    }

    getTree(options) {
        return options
    }
}